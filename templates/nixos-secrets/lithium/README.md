# lithium secrets

Host-specific secrets for `lithium` live here. Replace the placeholder files with your real secrets.

- `secrets.yaml`: SOPS-managed secrets
- `*.env`: service-specific env files
- `*-private.nix`: private Nix wiring for services
